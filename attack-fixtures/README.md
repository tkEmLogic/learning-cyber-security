# Attack fixtures

Tier 0 fixtures are implemented by the structured Go course helper.

`course.yml` owns every target, identifier, port, input, expected effect, reset command, and evidence location.

Run `./course attack list` and use only the printed fixture identifiers.
