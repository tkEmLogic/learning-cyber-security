# Tier 0 adds no configuration of its own.
#
# Everything the Tier 0 Reference product reads lives in firmware/common, which
# every tier shares. Tier 2 is the first tier to declare options here.

source "Kconfig.zephyr"
